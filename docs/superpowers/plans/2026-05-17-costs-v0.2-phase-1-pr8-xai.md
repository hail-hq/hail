# Costs v0.2 Phase 1 — PR8: xAI Grok

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add three xAI rows to `costs/llm.json` — Grok 4, Grok 3, Grok Code. Stage but do not commit.

**Architecture:** Single-file edit. No backfills. xAI operates own API (api.x.ai); native rates are canonical. Other deployment_options as verified.

**Source spec:** [`docs/superpowers/specs/2026-05-17-costs-v0.2-phase-1-design.md`](../specs/2026-05-17-costs-v0.2-phase-1-design.md)

---

## Critical: deprecation + canonical-ID check first

Pattern from PR5/PR6/PR7: subagents must verify deprecation status AND canonical model_id strings before drafting. Before any drafting:

- WebFetch `https://docs.x.ai/docs/models` for the canonical model_id strings + deprecation markers
- WebFetch `https://docs.x.ai/docs/pricing` (or wherever xAI publishes prices) for the current pricing table
- "Grok Code" may be a marketing name — the canonical API model_id may be different (e.g. `grok-code-1`, `grok-code-fast-1`)

If any model has been renamed or deprecated, adjust scope and document.

## Scope

3 new rows. Use canonical xAI API model_ids verified against docs.x.ai:

1. Grok 4 — likely `grok-4` or `grok-4-0709` style
2. Grok 3 — likely `grok-3` or `grok-3-mini` (verify which is the family flagship vs mini)
3. Grok Code — likely `grok-code-fast-1` or `grok-code-1`

## Per-row data

For each row:

- `provider: "xAI"`, `provider_url: "https://x.ai"`
- `model_id` (canonical API model_id verified against docs)
- `display_name` (e.g. "Grok 4", "Grok 3", "Grok Code Fast 1")
- `model_family` (e.g. "Grok 4", "Grok 3", "Grok Code")
- `knowledge_cutoff` (verify in xAI docs)
- `aliases[]` — undated/-latest forms if dated is canonical
- `context_window`, `max_output_tokens` from xAI docs
- `input_per_mtok_usd`, `output_per_mtok_usd` from xAI pricing (decimal strings)
- `cache_read_per_mtok_usd` — xAI publishes cached input rate; capture if present
- `cache_write_per_mtok_usd`, `batch_*`: UNSET unless xAI publishes them
- `pricing_tiers[]` — UNSET unless xAI has tiered pricing for long context
- `modalities`:
  - Grok 4 / Grok 3: verify vision support (Grok 4 likely supports image input)
  - Grok Code: text-only typically
- `supports_tool_use`, `structured_output`: verify per model
- `supports_vision`: per docs
- `supports_audio_in: false`, `supports_audio_out: false`, `supports_pdf` verify
- `reasoning_tokens_billed`: `true` for Grok 4 (thinking mode); verify Grok 3 (may have a reasoner variant)
- `deployment_options[]`: at least `["native"]`. xAI's API isn't on Bedrock/Vertex/Azure typically; verify.
- `confidence: high` default
- `last_verified: "2026-05-19"`, `last_changed_at` (actual or `"2026-05-19"`)
- `verification_method: "manual-confirmed"`, `verified_by: "r13i"`
- `source_url`: xAI's pricing page
- `notes`: 1-3 sentences (anything notable: Grok 4's thinking mode behavior, Code model's FIM specialization, etc.)

## Field order

Same as prior PRs.

## Steps (cap at ~4 WebFetch calls)

1. Read schema enum for `deployment_options`.
2. Read current state: `cat costs/llm.json | jq '.models[] | select(.provider == "xAI")'` (expect empty).
3. WebFetch `https://docs.x.ai/docs/models` — canonical IDs, capabilities, deprecation, knowledge cutoffs.
4. WebFetch `https://docs.x.ai/docs/pricing` (or `https://x.ai/api` if pricing lives elsewhere) — input/output/cache prices, batch availability.
5. Edit `costs/llm.json` — append 3 rows.
6. `pnpm costs:validate` — must pass.
7. jq referential checks — `[] ; replaced_by: []` per line.
8. `pnpm site:build` — must pass.
9. `git add costs/llm.json`.
10. Final report. PR description inline (no scratchpad file).

Commit message (do NOT execute):

```
feat(costs): add xAI Grok family rows

Adds grok-4, grok-3, and grok-code-fast-1 (or canonical equivalents).
xAI native rates as canonical structured prices; deployment_options
captures additional hosts where verified. Grok 4 carries
reasoning_tokens_billed: true (thinking mode). Grok Code is text-only
code-specialized.

Phase 1 PR8 of the costs v0.2 plan. Two-source rule applied per row.
```

## Self-review

- 3 new rows
- Canonical model_ids verified (don't invent strings)
- Deprecation status verified
- Decimal-string prices
- Aliases verified, not invented
- pnpm costs:validate / jq referential / pnpm site:build all pass
- costs/llm.json staged
- No `git commit` run
