{
  "name": "hail-hq",
  "version": "0.1.0",
  "private": true,
  "description": "Hail monorepo — tooling root (husky, lint-staged, prettier). Python and Go sources live in their own subdirs.",
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.1.0",
    "prettier": "^3.3.0"
  }
}
