{
  "name": "hail-hq",
  "version": "0.1.0",
  "private": true,
  "description": "Hail monorepo — tooling root (husky, lint-staged, prettier). Python and Go sources live in their own subdirs.",
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "prepare": "husky",
    "costs:validate": "pipx run check-jsonschema --schemafile costs/schema/llm.schema.json costs/llm.json && pipx run check-jsonschema --schemafile costs/schema/stt.schema.json costs/stt.json && pipx run check-jsonschema --schemafile costs/schema/tts.schema.json costs/tts.json && pipx run check-jsonschema --schemafile costs/schema/sms.schema.json costs/sms.json && pipx run check-jsonschema --schemafile costs/schema/numbers.schema.json costs/twilio.json costs/telnyx.json costs/didww.json",
    "costs:stale": "node scripts/costs/check-stale.mjs --max-age 30",
    "costs:featured": "node scripts/costs/check-featured.mjs",
    "costs:sync-numbers": "uv run python scripts/costs/sync_numbers.py --env-file .env",
    "site:dev": "pnpm --filter @hail-hq/web dev",
    "site:build": "pnpm --filter @hail-hq/web build",
    "site:start": "pnpm --filter @hail-hq/web start",
    "docs:dev": "pnpm --filter @hail-hq/docs-site dev",
    "docs:build": "pnpm --filter @hail-hq/docs-site build",
    "docs:urls": "node docs-site/scripts/urls.mjs",
    "docs:check-urls": "node docs-site/scripts/urls.mjs --check"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.1.0",
    "prettier": "^3.3.0"
  }
}
